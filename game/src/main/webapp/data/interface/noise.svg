<svg id="svgNoiseTest" viewBox="0 0 200 200" xmlns='http://www.w3.org/2000/svg'>
  <filter id='noiseFilter'>
    <feTurbulence 
      type='fractalNoise' 
      baseFrequency='0.465' 
      numOctaves='3' 
      stitchTiles='stitch' />
  </filter>
  <filter id="filter1">
			<!--feMorphology operator="dilate" in="SourceGraphic" radius="0.75" out="test"/-->
			<feTurbulence type='fractalNoise' baseFrequency='0.265' numOctaves='3' stitchTiles='stitch' />
			<feGaussianBlur stdDeviation="3" />
			
            
        </filter>
  <rect id="background" width='100%' height='100%' filter='url(#filter1)' />
</svg>